// Add to schema.prisma

model MessageCache {
  id          String   @id @default(cuid())
  hash        String   @unique
  messageId   String
  workspaceId String
  createdAt   DateTime @default(now())

  @@index([workspaceId])
  @@map("message_cache")
}
