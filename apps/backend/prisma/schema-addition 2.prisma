// ============================================================
// SCHEMA ADDITION — append to apps/backend/prisma/schema.prisma
// ============================================================
//
// 1. Add the model below to your schema.
//
// 2. Add these relation fields to existing models:
//
//    model Client {
//      ...
//      shareTokens  ClientShareToken[]
//    }
//
//    model Workspace {
//      ...
//      shareTokens  ClientShareToken[]
//    }
//
// 3. Ensure ClientPropertyStatus enum includes PENDING:
//
//    enum ClientPropertyStatus {
//      PENDING
//      INTERESTED
//      NOT_INTERESTED
//      SHORTLISTED    // keep any existing values
//    }
//
// 4. Run: npx prisma migrate dev --name add_client_share_token
// ============================================================

model ClientShareToken {
  id          String    @id @default(cuid())
  token       String    @unique @default(uuid())
  clientId    String
  workspaceId String
  expiresAt   DateTime
  createdAt   DateTime  @default(now())
  viewedAt    DateTime?

  client      Client    @relation(fields: [clientId], references: [id], onDelete: Cascade)
  workspace   Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)

  @@index([clientId])
  @@index([workspaceId])
}
